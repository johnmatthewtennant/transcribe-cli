# Transcribe Audio Setup

1. Install the CLI:
```bash
brew install johnmatthewtennant/tap/swift-transcribe
```

Requires macOS 26+ (Tahoe) and Apple Silicon.

2. On first run, grant Microphone and Screen Recording permissions when prompted.

3. Verify: `transcribe --help` should show usage.
